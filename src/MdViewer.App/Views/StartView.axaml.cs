using Avalonia.Controls;

namespace MdViewer.App.Views;

/// <summary>
/// The start view: shown when no tabs are open, and the primary home of the
/// recent-documents list (SPECIFICATION.md 5.14).
/// </summary>
public partial class StartView : UserControl
{
    public StartView()
    {
        InitializeComponent();
    }
}
